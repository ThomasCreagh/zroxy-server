worker_processes auto;
events { worker_connections 8192; }

http {
    sendfile on;
    tcp_nopush on;
    tcp_nodelay on;

    access_log off;

    server {
        listen 8080;
        root html;
        index index.html;
    }
}
