worker_processes auto;

events {
    worker_connections 8192;
}

http {
    access_log off;
    error_log /dev/null crit;

    server {
        listen 8080;
        location / {
            return 200 "OK";
        }
    }
}
